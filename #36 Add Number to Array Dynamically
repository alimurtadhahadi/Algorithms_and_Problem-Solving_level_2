/*
=================================== ⚙️ ﷽ ===================================
  [PROBLEM] │ #36-Add Number to Array Dynamically
  ----------|---------------------------------------------------------------
  [CONTEXT] │ Course: ProgrammingAdvices (Algorithms Level 2)
  ----------|---------------------------------------------------------------
  [AUTHOR]  │ Ali_Dev (@Ali_DevCode_Lab)
  ----------|---------------------------------------------------------------
  [DATE]    │ June 26, 2026 (Friday)
  ----------|---------------------------------------------------------------
  [DESC]    │ Simulates a dynamic vector insertion pipeline using an integer-based
  ----------| sentinel validation loop to safely track and expand stack array sizes.
  ----------|---------------------------------------------------------------
  [URL]     │ TG: t.me/Ali_DevCode_Lab │ YT: youtube.com/Ali_Dev_Hub │ GH: github.com/alimurtadhahadi
===========================================================================
*/
#include <iostream>
#include <string>
using namespace std;

void AddArrayElement(int Number, int Arr[100], int& ArrLength)
{
    ArrLength++;
    Arr[ArrLength - 1] = Number;
}

int ReadNumber()
{
    int Number;

    cout << "Please enter a number : ";
    cin >> Number;

    return Number;
}

void InputUserNumbersInArray(int Arr[100], int& ArrLength)
{
    short AddMore = 1;

    do
    {
        AddArrayElement(ReadNumber(), Arr, ArrLength);

        do
        {
            cout << "Do you want to add more numbers? [0]:No, [1]:Yes : ";
            cin >> AddMore;
        } while (AddMore != 0 && AddMore != 1);

    } while (AddMore == 1);
}

void PrintArray(int Arr[100], int ArrLength)
{
    for (int i = 0; i < ArrLength; i++)
    {
        cout << Arr[i] << " ";
    }
    cout << endl;
}

int main()
{
    int Arr[100];
    int ArrLength = 0;

    InputUserNumbersInArray(Arr, ArrLength);

    cout << "\nArray Length: " << ArrLength << endl;
    cout << "Array elements: ";
    PrintArray(Arr, ArrLength);

    return 0;
}
